# Company Accounts & Pricing — phased implementation plan

## Context

The user supplied three reference documents for a new internal admin module:
1. A static HTML/CSS admin mockup bundle (13 pages, rougher/earlier-pass visual style).
2. `COMPANY_ACCOUNTS_AND_PRICING_BACKEND_SPEC.md` (612 lines) — the functional requirements: company management, external-user roles + invitation workflow, two pricing methods (assigned predefined price list, and per-company item overrides), a 3-tier pricing priority, price-list versioning with order-time price freezing, price-visibility rules for external customers, required screens, an internal permissions matrix, and audit history.
3. 14 polished 1600×1200 PNG screenshots, styled to match the real live app ("mySPEEDPANEL" branding) — the **definitive visual/UX reference**, superseding the HTML mockup. These show a new left-sidebar "Account Management" workspace (Companies / Company Users / Invitations / Company Pricing / Price Lists / Permissions / Audit History) sitting alongside the existing Estimator/Projects nav.

A large amount of what the spec describes **already exists and works** in this codebase — companies, company memberships, invitations, staff assignment, dynamic RBAC, audit logging, and single-list price assignment are all real, live features (`supabase/schema.sql`, `src/pages/admin/companies/`, `src/pages/company/`, `src/pages/admin/priceLists/`). This is **not a rebuild** — it's a scoped, additive extension of one domain (companies + pricing), reusing existing infrastructure everywhere it already satisfies the spec, and adding new tables/RPCs/pages only for genuine gaps. The three genuine backend gaps are: **per-company item price overrides** (zero backend today), **price-list versioning** (today's `price_lists` is one flat, live-edited row with no history), and **multi-address support** (today `companies.address` is a single text field). A fourth gap — order-time price freezing — is real but smaller than first thought, since `order_deliveries` already freezes addresses as plain text columns (no FK to unfreeze).

User confirmed (AskUserQuestion): build the new module as its **own top-level workspace** matching the screenshots exactly; retire the old `#/admin/companies` / `#/admin/priceLists` / (raw-grant) `#/admin/roles` pages once their new-module equivalents reach parity, in a final cleanup phase — `AdminRolesPage.tsx` itself is kept permanently as the underlying raw-grant editor, not replaced, since the new "Access Permissions" page is a curated summary view reading the same `role_permissions` data, not a different grant model.

This plan is intentionally phased for execution across many separate future sessions (not one sitting) — mirrors this codebase's own precedent (`docs/unified-estimator-merge-plan.md`'s Phase 1–6 structure for the estimator merge). Commit after each phase passes its own verification; don't batch phases into one commit.

## Corrected understanding from deep research (read this before starting any phase)

- **The admin dashboard tile grid is already orphaned for this area.** `adminSections.tsx`'s `ADMIN_GROUPS` has exactly one group today ("Workflow": Projects Administration/Orders/Delivery Requests) — Companies/Price Lists/Permissions routes still work via direct `#/admin/...` URLs but have zero dashboard tiles pointing at them. There is no "current experience" to visually integrate the new module with beyond raw URLs.
- **"One active primary user required" already exists server-side** as `company_set_member_role`/`company_set_member_status`/`company_remove_member`'s existing refusal to demote/suspend/remove the last remaining `owner`. Spec's "Primary User" is just a display label for the existing `owner` `CompanyRole` — no new role, no new guard, just UI copy (`COMPANY_ROLE_LABELS`).
- **`src/export/applyEffectivePricing.ts`** is the single load-bearing file for Method 2 — it already implements the assigned-list → default-list fallback chain; adding the item-override tier in front of it is the one-line change the whole feature builds toward. Audit every current caller (client-side pricing preview, order pricing, Excel export), not just the obvious one.
- **`price_lists`/`price_list_prices` today has no version concept whatsoever** — `admin_set_price_list_price` edits a live row in place, immediately affecting every assigned company. Versioning is a real behavioral change to a live, working, in-place-edit RPC and page (`AdminPriceListsPage.tsx`), not just new tables bolted on alongside.
- **Order creation is a bare RLS insert, not an RPC** (`"Owners can create their own orders"` policy, schema.sql ~1136–1180) — only `revise_order` (an existing RPC, staff-only, submitted/proforma_requested stages only) recomputes totals server-side, and even it doesn't validate against price lists. This matters for Phase 10: there's no existing RPC boundary to add price re-verification into for *initial* order creation.
- **`order_deliveries` already freezes addresses** as plain text columns (`address_line1/2`, `suburb`, `state`, `postcode` — schema.sql ~1191) with no FK to any address entity. "Freeze the address used" is a solved problem already, not a gap — `company_addresses` (Phase 3) is purely a *picker source* for populating those text columns at delivery-creation time; no additional freeze work is needed in Phase 10.
- **No cron infrastructure exists anywhere** (`pg_cron` not enabled, only `pgcrypto` is; grep confirms zero `cron.schedule` usage) and **no "date has passed" lazy-check pattern exists either** (`invitations.expires_at` is set but never actually compared against `now()` in any view/function). "Scheduled" price-list versions auto-activating on their effective date has no precedent to follow — recommend a **lazy on-read check** (a view/function comparing `effective_date <= current_date`) over introducing `pg_cron` as a new dependency, but this is a real design choice to make explicitly in Phase 8, not follow convention on.
- **No `supabase/migrations/` directory** — `schema.sql` is one monolithic declarative file; new permission keys are appended as new `insert into public.permissions (...) values (...) on conflict (key) do nothing;` blocks near each new feature's own section (6 such blocks already exist, scattered through the file) — follow that exact pattern, don't invent a migrations folder.
- **CSV import/export (`priceListCsv.ts`) is schema-agnostic and directly reusable** for Phase 7's draft-version editor — it never touches Supabase itself (pure parse/build functions over `PriceListPriceRow[]`), matches by `Product ID` + `Category` (name column is cosmetic/ignored on import), and export uses `XLSX.writeFile(..., {bookType:"csv"})` for a direct browser download (no manual Blob/anchor code needed). Only `priceListsStore.ts` (which parameterizes reads/writes by `price_list_id`) needs updating to target `price_list_version_id` instead.
- **Test infrastructure to hook into, with exact commands:**
  - pgTAP: `supabase/tests/database/01_..07_...test.sql` (numbered, run together via `npm run test:rls` → `supabase test db`, against the local Docker Supabase stack, real RLS via `set_config('request.jwt.claims', ...)` — not a service-role bypass). New phases add `08_...test.sql` onward. `02_company_isolation.test.sql` is the representative company/permissions example (`plan(10)`, `results_eq`/`throws_ok`-style cross-company isolation checks).
  - Playwright: `e2e/*.spec.ts` (19 files, incl. `cross-company-isolation.spec.ts`, `admin-invite-flow.spec.ts`, plus one spec per role under `e2e/roles/`), run via `npm run test:e2e` → `playwright test`, against a **production build** (`npm run preview`) and a **real/remote** Supabase project (not local Docker), authenticating as fixed seeded `@e2e.test` accounts via `e2e/fixtures/auth.ts`. CI (`.github/workflows/e2e.yml`) runs this on every PR but with `continue-on-error: true` — it does **not** gate merges today (pre-existing CI/auth environment issue), so don't treat a red e2e run as a hard blocker, but do still extend the suite for new isolation-sensitive behavior (new override/pricing endpoints).
  - Neither suite runs automatically via CLAUDE.md's standard `typecheck && test && build` — both are separate, explicit commands to run per phase.

## Open decisions resolved from the spec/screenshots directly (flag if wrong when reviewing)

- **`companies.status`**: expand to the spec's exact 5-state list — Pending / Active / On Hold / Suspended / Archived. Spec §1 states On Hold and Suspended both "prevent new orders but should not remove access to existing projects and order history" — treat them as functionally identical for order-blocking purposes; On Hold additionally gets the reason/review-date/hold-details tracking the screenshot shows (screen 14), Suspended does not need to (no such screen was shown for it).
- **Override "Approved by"**: spec §6 lists it as a stored field, and neither the spec text nor the screenshot describes a pending-approval gate (override status shown is only Active/Scheduled/Expired, derived from dates) — treat `approved_by`/`approved_at` as audit-only metadata set at creation/edit time by whoever has permission, not a second workflow state. The Access Permissions screenshot's BDM row showing "Request" for Overrides suggests a lower-privileged role can *request* one but not create it directly — worth a lightweight `pending_review` concept only if BDM-initiated requests turn out to be in scope; default to skipping it (Internal Sales/Super Admin only can write overrides directly per the permissions grid).
- **Invitation expiry default**: mockup screen 07's "Invitation Rules" panel explicitly states "expires after five days" — change the schema default from 14 days to 5.
- **Access Permissions page**: a curated read view (screen 11's Full/View/Assigned/Request/Limited/Hidden/No labels per role/capability), backed by existing `role_permissions`/`has_permission()` data via a new client-side `capabilityLabelMap.ts`, not a new grant model and not a rewrite of `AdminRolesPage.tsx`.
- **Order/quote price-freeze fields**: spec §9's list (product description/code, qty, unit, unit price, price source, discount, tax, line total, price-list version, override used) maps onto additive fields in the existing `orders.line_items` jsonb array element shape, plus one new `orders.price_list_version_id` column — not a new snapshot table, consistent with this schema's existing "whole array as one jsonb column" convention (see the comment already on `orders.line_items`).

---

## Phase 1 — Workspace shell, routing, Control Room — DONE

**Scope.** New top-level workspace nav + shell + a Control Room dashboard reading only data that already exists (company/user/invitation/price-list counts). Action-queue items depending on not-yet-built data (override expiring soon, price list ready to publish) render empty/hidden with an inline comment noting which later phase wires them for real — never fake data.

**What actually shipped.**
- `src/appShell/useHashRoute.ts`: new `AccountsSubPage` union (`controlRoom | companies | companyUsers | invitations | companyPricing | priceLists | permissions | auditHistory`), `{ tab: "accounts"; sub: AccountsSubPage }` `Route` variant, `ACCOUNTS_SUBPAGES` array, `parseHash`/`routeToHash` cases mirroring `AdminSubPage`'s handling exactly.
- **IA decision made concretely, not just discussed**: "admin" is deliberately excluded from `topNav.tsx`'s `TOP_NAV_ITEMS` (reached via `AuthStatus.tsx`'s account dropdown instead) — the new workspace follows that exact same precedent rather than adding a `TOP_NAV_ITEMS` entry. `TopNavTab` gained `"accounts"` purely for type-compatibility with `route.tab` (same reason `"admin"`/`"company"`/`"myRequests"` are already in that union without ever matching a nav button). `AuthStatus.tsx` gained a second staff-only dropdown item, "Company Accounts & Pricing" (`Building2` icon), right below "Admin"; the dropdown widened from `w-48` to `w-60` to fit the longer label without an awkward wrap.
- `src/pages/accounts/AccountsRoot.tsx` — a hybrid of `AdminRoot.tsx`'s pattern (route.sub-driven dispatch, `AdminGate`-gated on `isInternalStaff`) and `AdminProjectsAdministrationPage.tsx`'s pattern (a persistent left sidebar owned by the page itself, since every one of this workspace's 14 screens shares the identical sidebar, unlike Admin's per-section-owned sidebars). One file does both — no separate `AccountsSidebar.tsx` was worth splitting out, since the nav config/JSX is only ever used here. Per-section `admin.section.*`-style permission gating is deferred (the plan itself says this is optional for Phase 1); the whole workspace is gated on `isInternalStaff` only for now, same as `AdminGate`.
- `src/pages/accounts/accountsTheme.css` — **a lighter-weight scoped theme than the other ported admin themes on purpose**: unlike `.pa-shell`/`.est-shell` (each sourced from a *separate* UI-DESIGNS mockup bundle with genuinely different hex values from the site's own brand), this module's screenshots are the live app's *own* real branding ("uses the supplied desktop open-project page as the visual reference") — so `.cap-shell` does NOT shadow `--navy`/`--blue`/etc with an invented palette. It reads the site-wide tokens directly and only adds new component classes for shapes with no existing equivalent: `.cap-side`/`.cap-nav` (closely mirrors `.pa-side`/`.pa-nav`'s dark-rail-with-active-highlight shape) and `.cap-kpi`/`.cap-queue-item`/`.cap-allocation-item` (screenshot-specific tile/list shapes). Still imports `ui/scopedThemeTokens.css` for the handful of genuinely shared `--sp-*` tokens (surface/line/muted/bg colours). Built incrementally — only what Phase 1's Control Room needs exists so far; later phases add their own classes as their screens get built, not speculatively now.
- `src/pages/accounts/ControlRoomPage.tsx` + `controlRoomStore.ts` — real KPI tiles (active companies, external users, pending invitations, active price lists) and a real Price List Allocation breakdown, all read via plain Supabase queries (confirmed live: `companies`/`company_memberships`/`invitations`/`price_lists` each already carry an `or public.is_admin()` clause on their own SELECT RLS policy, so no new RPC was needed for Phase 1, matching the plan's own "Backend: None" expectation). The screenshots' "Module Owner"/"Pricing Administrators" owner cards and the override-expiring/price-list-ready action-queue items were deliberately omitted rather than faked — there's no per-module ownership-assignment concept in the backend, and no override/version data exists yet (Phases 6–9). Only "N invitations pending" and "N companies on hold" (using today's existing 3-value status enum, ahead of Phase 2's 5-value expansion) are shown, each with an inline comment marking where the deferred items get wired in later.
- `src/App.tsx` — `AccountsRoot` lazy-loaded the same way `AdminRoot` is (separate bundle chunk, confirmed in the build output: `AccountsRoot-*.js`/`.css` split cleanly from the main bundle and from `AdminRoot`'s own chunk), rendered for `route.tab === "accounts"` inside the same `<Suspense>` pattern.
- Reused `PlaceholderPage.tsx` (the app's existing "not-yet-built admin route" stub) for the other 7 sub-pages, each with a description naming which later phase replaces it — swapped out page-by-page as those phases land, same convention that component already documents for the Admin section.

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`): account-dropdown shortcut opens the workspace; all 8 sidebar nav items present and route correctly (`#/accounts`, `#/accounts/companies`, etc.); active-item highlighting follows `route.sub`; KPI tiles render real live counts (not placeholders); placeholder sub-pages render without crashing; light AND dark mode both screenshotted and correctly themed; responsive breakpoint (`.cap-layout` collapses to a single column below 900px) confirmed with zero horizontal overflow at 820px width. Full verification suite (typecheck/test/build/depcruise) clean, 187 tests passing, 0 new dependency-cruiser errors (28 pre-existing warnings unchanged).

---

## Phase 2 — Companies list + Company Overview shell + companies schema catch-up — DONE

**Scope.** Real Companies list + Company Overview page (Overview tab only — other tabs are placeholders here, built out in later phases). Extend `companies` with whatever Account-step fields the spec defines that don't exist yet and expand `status` to the 5-state model — **schema only, no enforcement wiring yet** (that's Phase 11, kept separate on purpose).

**What actually shipped.**
- **Schema** (new "Company Accounts & Pricing -- Phase 2 schema catch-up" section in `supabase/schema.sql`, right before the company-creation section): `companies.status` expanded from `active|suspended|closed` to `pending|active|on_hold|suspended|archived` (existing `closed` rows backfilled to `archived`, since no code path ever set `closed`); two new nullable columns, `payment_terms`/`internal_notes`. New `admin_set_company_status(p_company_id, p_status, p_reason)` RPC (`has_permission('companies.set_status')`-gated, new permission key, logs a `company_status_changed` audit event with `{from, to, reason}` — added to `EVENT_TYPE_LABELS`). `admin_create_company` extended additively with `p_payment_terms`/`p_internal_notes` (dropped + recreated, not a bare `create or replace`, since appending params changes the signature — same convention `admin_list_staff_candidates()` already used) and now inserts new companies as `status = 'pending'` (was implicitly `'active'` before). New `admin_company_activity_counts(p_company_id)` RPC (project/order counts for the Overview KPI tiles, orders reached via its `project_id -> projects.company_id` join since orders has no `company_id` of its own). `admin_list_companies()` grew from a 4-column row to carry every field both new screens need (status, account code, ABN, billing/phone/address, payment terms/notes, price list id+name, primary user name+email, internal owner name) — **defined further down in the file, after the `price_lists` section**, not at its original spot, since `language sql` function bodies (unlike `plpgsql`) are checked against referenced-object existence at `CREATE` time, and it now reads `price_lists.name`.
- **`src/pages/accounts/companies/CompaniesListPage.tsx`** — real company table (`ui/table.tsx`'s shared `Table`, not a hand-rolled one) with search (name/account code/primary user) + status filter + price-list filter, all client-side over the already-loaded rows (small internal B2B list, same scale assumption `AdminProjectsBrowserPanel.tsx`'s search makes). Status pills use the existing `cx.badge`/`tone()` convention, not new CSS.
- **`src/pages/accounts/companies/CompanyWizard.tsx`** — the screenshots' 6-step wizard, but only the first 3 are real (`Company` / `Account` / `Primary User`); steps 4-6 (`Addresses`/`Pricing`/`Review`) render as dimmed "coming in a later phase" chips in the step indicator rather than being faked, since `company_addresses` (Phase 3) and `company_price_overrides` (Phase 9) don't exist yet. Repurposes `AdminCompanyWizard.tsx`'s existing 3 steps, **reordered** to match the screenshots: `Company` (all company-level fields incl. the two new ones, one `admin_create_company` call) → `Account` (`StaffTeamAssignmentPanel.tsx` reused verbatim — the screenshots show "Internal owner" already set by the time Primary User is reached, i.e. owner-assignment happens *before* the customer invite) → `Primary User` (a single `role='owner'` invite, restricted from the old multi-invite `CustomerUsersStep`, matching "One primary user is required").
- **`src/pages/accounts/companies/CompanyOverviewPage.tsx`** — header (name, status badge, inline `StatusChanger` using `admin_set_company_status`), 4 real KPI tiles (active users, assigned price list, open projects, orders — the latter two via `useCompanyActivityCounts`), Company Account + Contact Details cards, an Internal Notes card (only rendered when notes exist), and an 8-tab bar (`ui/tabs.tsx`) where only Overview is real — Users/Pricing/Addresses/Projects/Quotes/Orders/Audit all render the existing `PlaceholderPage` stub naming which later phase replaces them, per Phase 2's explicit "Overview tab only" scope (even though `CompanyMemberList.tsx` already exists and could have been wired in early — deferred anyway to keep phase boundaries clean, per Phase 4's own scope).
- **`src/pages/admin/companies/companiesStore.ts`** extended in place (not duplicated) — `AdminCompanyRowSchema` grew to match the new RPC row shape, `useAdminCreateCompany` takes the two new optional fields, plus new `adminSetCompanyStatus()` and `useCompanyActivityCounts()`.
- **`src/appShell/useHashRoute.ts`** — the `accounts` route variant grew `companyId`/`newCompany` fields (only meaningful when `sub === "companies"`), giving `#/accounts/companies`, `#/accounts/companies/create`, and `#/accounts/companies/:id`.
- **`src/pages/company/companyTypes.ts`** — `company_status_changed: "Company status changed"` added to `EVENT_TYPE_LABELS`.
- New `supabase/tests/database/08_company_status.test.sql` (6 pgTAP assertions: super_admin can change status + audit log recorded with from/to/reason, invalid status rejected, ungranted role denied, `admin_create_company` defaults to `pending`). `05_execute_grants.test.sql` updated for `admin_create_company`'s new 9-arg signature plus 2 new `ok(has_function_privilege(...))` lines for the two new RPCs (`plan()` bumped 68 → 70).

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`): Companies list loads real rows with correct status/pricing/owner columns, search/filters work; wizard creates a company end-to-end through all 3 real steps and lands on its Overview; Overview's KPI tiles, tabs, and inline status-change control all work against real data; a placeholder tab (Addresses) renders its "coming in Phase 3" copy without crashing; light and dark mode both screenshotted and correct. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors). `npm run test:rls`: the new/modified files (`08_company_status.test.sql`, `05_execute_grants.test.sql`) pass cleanly, as do the untouched `01`/`03`/`04` files; `02`/`06`/`07` have pre-existing failures confirmed unrelated to this diff (verified via `git diff` — zero touches to `order_deliveries`, `role_permissions`, or company-isolation logic) and traced to this sandbox's from-scratch local DB bootstrap (no `supabase/migrations/` directory means `schema.sql` isn't wired for `supabase db reset` to apply automatically here, and reconstructing the baseline table/function grants by hand doesn't perfectly reproduce the real project's organically-accumulated grant history) rather than to any code in this phase.

**Dependencies.** Phase 1.

---

## Phase 3 — Company Addresses — DONE

**Scope.** New `company_addresses` table (billing/delivery/office typing, default flags, delivery-contact fields) + Addresses tab on Company Overview + the wizard's Addresses step. This is genuinely new — today `companies.address` is one text field. Existing `order_deliveries` freeze behavior (Phase note above) needed zero changes — this table is purely a picker source for pre-filling that form.

**What actually shipped.**
- **Schema** (new "Company Accounts & Pricing -- Phase 3: Company Addresses" section in `supabase/schema.sql`, right after Phase 2's `admin_company_activity_counts`): `company_addresses` table matching this section's own draft SQL, **plus one deliberate addition beyond it** -- a `label text` column. The draft schema had no way to distinguish two addresses of the same type (the screenshots show "Dandenong Warehouse" and "Sydney Office" as two distinct saved locations), and the backend spec's own table list names `company_addresses` without giving a column list of its own, so this was a judgment call, not a spec violation. `company_addresses_one_default_per_type` unique partial index enforces one default per `(company_id, type)` at the DB level. **Two separate access paths**, both as planned: RLS gives the company's own active members read access and `is_company_admin(company_id)` write access (a future customer-facing addresses page could use this directly, though none exists yet); the 4 new RPCs (`company_list_addresses`, `admin_set_company_address` upsert, `admin_delete_company_address`, `admin_set_default_address`) are the STAFF module's path instead, gated by the new `company_addresses.read`/`company_addresses.write` permission keys and the only path that calls `log_audit()` (`company_address_added`/`changed`/`removed`, added to `EVENT_TYPE_LABELS`). `admin_set_company_address` clears any existing default for that type in its own statement before inserting/updating the new one, so the unique index's constraint check (immediate, not deferred) never sees two defaults at once even transiently.
- **`src/pages/accounts/companies/companyAddressesStore.ts`** + **`CompanyAddressesTab.tsx`** — one reusable block, not two implementations: `CompanyOverviewPage.tsx`'s Addresses tab renders it directly, and `CompanyWizard.tsx`'s new step 4 embeds the exact same component. Address cards show label/type badge/default badge, with Edit/Delete/"Set as default" (star icon) actions; an inline form (shared for create and edit) handles label/type/street/suburb/state/postcode/delivery-contact fields. The screenshots' "Address Usage" card (recent transactions using saved addresses) was deliberately omitted — `order_deliveries` freezes addresses as plain text with no `address_id` FK, so there's no real per-address transaction history to show, only what would be fabricated.
- **`CompanyWizard.tsx`** grew a real 4th step (`Addresses`, embedding `CompanyAddressesTab`) — `REAL_STEPS` now covers `Company`/`Account`/`Primary User`/`Addresses`, `FUTURE_STEPS` is down to just `Pricing`/`Review` (Phase 9). Primary User's "Finish" button was relabelled "Continue" now that a real step follows it.
- **`CompanyOverviewPage.tsx`** — the Addresses tab is real now (was a `PlaceholderPage`); the Overview tab's stale "coming in Phase 3" footnote was updated to point at the new tab.

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`): Addresses tab create/edit/delete all work against real data; adding a second default address correctly clears the first's default flag (both in the UI and confirmed via pgTAP against the raw table); the wizard's new Addresses step adds an address and finishes onto the real company overview; light and dark mode both screenshotted and correct. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors). `npm run test:rls`: new `09_company_addresses.test.sql` (11 assertions: create/clear-old-default/raw-insert-still-rejected-by-the-unique-index/cross-company-isolation/permission-denied-for-an-ungranted-role/delete) and the extended `05_execute_grants.test.sql` both pass cleanly; the same 3 pre-existing failures from Phase 2 (`02`/`06`/`07`, confirmed via `git diff` to be outside every phase's diff) are unchanged.

**Dependencies.** Phase 2.

---

## Phase 4 — Company Users tab — DONE

**Scope.** Restyle `CompanyMemberList.tsx` (308 lines, already implements roster/invite/role-change/suspend/remove/resend against real data) into the new module's Users tab — overwhelmingly reuse, minimal-to-no new backend.

**What actually shipped.**
- **Backend.** None, confirmed — `CompanyMemberRowSchema` already returns `assigned_project_count`, matching the mockup's "Project Access" column exactly (shown as body text under each member's name, not a separate table column, per `CompanyMemberList.tsx`'s existing `MemberRow` — see the "restyle not rebuild" note below for why that stayed as-is).
- **`src/pages/accounts/companies/CompanyUsersTab.tsx`** — a thin wrapper, not a fork: renders `CompanyMemberList.tsx` verbatim with `canManage={true}`/`isSpeedpanelAdmin={true}` (same as the old `AdminCompaniesPage.tsx`'s "Members" accordion), plus one line of intro copy. `CompanyMemberList.tsx` itself was **not modified at all** — its card-based roster/invite-form/pending-invitations layout stayed exactly as-is rather than being rebuilt into the mockup's table-based "Company Users" screen, per the plan's own explicit "same component, new container styling — not a fork" instruction.
- **`CompanyOverviewPage.tsx`** — the Users tab is real now (was a `PlaceholderPage`); gained a `myUserId` prop (threaded from `AccountsRoot.tsx`'s `auth.user?.id`, needed so `CompanyMemberList` can mark "(you)" and hide self-management controls correctly) and the Overview tab's "Manage company users" link now switches to the local Users tab instead of navigating to the still-unbuilt standalone cross-company "Company Users" sidebar page.

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`): the Overview tab's "Manage company users" link correctly switches to Users; both seeded members render; role change (Estimator → Viewer → back) and suspend/reactivate both work end-to-end against real RPCs; the "Add existing account" form (staff-only) renders. **One gap, documented rather than silently skipped**: invite/resend/cancel all go through the `company-invite-member` Edge Function, and this sandbox's Docker cannot run the edge-runtime container at all (`supabase functions serve` itself fails with a rootless-Docker rlimit error, confirmed directly, not assumed) — so the actual invite-send round-trip isn't exercised here. This is a sandbox limitation, not new to this phase (`CompanyMemberList.tsx`'s invite code is unmodified pre-existing functionality); role-change/suspend/reactivate (real RPCs, no Edge Function) cover the "did wrapping this in new chrome silently break a click handler" risk this phase's own verification note calls out. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors). No new pgTAP file (no new backend); `npm run test:rls` re-run as a sanity check, same result as Phase 3 (all touched files pass, same 3 pre-existing unrelated failures).

**Dependencies.** Phase 2.

---

## Phase 5 — Standalone Invitations page + "Delivery Failed" status — DONE

**Scope.** Cross-company Invitations page (today invitations only surface per-company) + a genuinely new "Delivery Failed" invitation status (confirmed absent from both the schema and the Edge Function's error handling — a real send failure currently deletes the just-inserted row and returns a synchronous error, nothing persisted).

**What actually shipped.**
- **Schema** (new "Company Accounts & Pricing -- Phase 5" section in `supabase/schema.sql`, right after `decline_company_invitation`): `invitations` ALTERed (not edited into the original `create table if not exists` — same reasoning as every other phase's schema catch-up) to add `failure_reason text` and expand the status check constraint to include `delivery_failed`. Default `expires_at` changed from 14 to 5 days (matching the mockup's "Invitation Rules" copy); `resend_company_invitation`'s own hardcoded 14-day extension updated to match. New `admin_list_invitations(p_company_id, p_status)` RPC (cross-company, new `invitations.list` permission key — every other invitation RPC is scoped to one company via `is_company_admin()`, this is the first cross-company one). New `admin_fix_invitation_email(p_invitation_id, p_new_email)` RPC (`is_company_admin()`-gated like its siblings, not `invitations.list` — fixing one company's invitation is a per-company write; only accepts a currently-`delivery_failed` row, resets status/email/failure_reason/expiry, logs a new `invitation_fixed` audit event).
- **`supabase/functions/company-invite-member/index.ts`**: the initial-send and resend paths' real-failure branches now `UPDATE` the row to `delivery_failed` + `failure_reason` instead of deleting it. New third `action: "fixEmail"` mode (calls `admin_fix_invitation_email()` for the metadata half, then re-fires `inviteUserByEmail()` at the corrected address — same two-step split as the existing "resend" mode). **`admin-invite-user/index.ts` deliberately NOT touched**, despite the plan's own parenthetical — traced its actual code and confirmed it never creates an `invitations` row at all (it creates the `auth.users` account directly and, for a company invite, inserts straight into `company_memberships`), so there's no row for it to transition to `delivery_failed`; the plan's "(and admin-invite-user)" was imprecise.
- **`src/pages/accounts/invitations/invitationsStore.ts`** + **`InvitationsPage.tsx`** — real cross-company table (invitee/company/role/status/sent/expires + per-status actions: Resend+Cancel for pending, Fix Email for delivery_failed, Open company for expired), KPI tiles (Pending/Delivery Failed/Accepted this month/Expired), search + status filter. Resend/cancel reuse the exact same Edge Function/RPC calls `company/companyStore.ts`'s `useCompanyMembers` already makes (not duplicated, just called from a second place). **No standalone "Create Invitation" form** — an invitation is always scoped to one company + role, and that form already exists per-company (`InviteMemberForm` inside `CompanyMemberList.tsx`, Phase 4's Users tab); the page's "Create Invitation" button routes to the Companies list rather than duplicating a company-picker + invite form the plan's own Frontend bullet list didn't call for.
- **`src/pages/accounts/AccountsRoot.tsx`** — Invitations sub-page is real now (was a `PlaceholderPage`); Control Room's "invitations pending" queue item already linked here since Phase 1, now lands on a real page.
- New `supabase/tests/database/10_invitations.test.sql` (10 pgTAP assertions: cross-company read + status filter, fix-email rejected for a non-delivery_failed row, fix-email succeeds and resets status/email/failure_reason, cross-company isolation on the list RPC, permission denied for a non-admin). `05_execute_grants.test.sql` extended for the 2 new RPCs.

**One deliberately deferred verification item.** The plan's own verification note calls for extending `e2e/admin-invite-flow.spec.ts` with a forced-failure case. Not attempted: this sandbox's Docker cannot run the edge-runtime container at all (confirmed directly in Phase 4 — `supabase functions serve` fails on a rootless-Docker rlimit restriction), so there's no way to locally verify a real `inviteUserByEmail()` failure path, and writing untested e2e spec code against a live remote project without being able to run it locally risks shipping something subtly wrong. The schema/RPC/Edge-Function logic is covered by `10_invitations.test.sql` and a live Playwright pass using fixture rows instead (see below); the actual Auth-level failure path remains genuinely unverified until it can be exercised in an environment with a working edge-runtime (e.g. CI's Path C, or a future session with Docker access).

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`, with 3 fixture invitation rows inserted directly since the create/resend/fix-email flows all ultimately need the unavailable Edge Function): all 3 rows render with correct status badges; KPI tiles compute correctly; search and status filters both narrow correctly; the Fix Email panel opens pre-filled with the current email and shows the failure reason; Cancel on a pending invitation works end-to-end via the direct RPC (no Edge Function needed) and the row updates to Cancelled; the expired row's "Open company" navigates to that company's Overview; light and dark mode both screenshotted and correct. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors). `npm run test:rls`: `10_invitations.test.sql` and the extended `05_execute_grants.test.sql` both pass cleanly; the same 3 pre-existing unrelated failures from every prior phase are unchanged.

**Dependencies.** Phase 1 only — independent of Phases 2–4, can run in parallel across sessions if useful.

---

## Phase 6 — Price-list versioning schema (backend only, no new UI) — DONE

**Scope.** The highest-risk phase — restructure `price_lists`/`price_list_prices` from one flat, live-edited row into a versioned model, **while keeping the existing `AdminPriceListsPage.tsx` working throughout** (it's live and used today; this is a hard backward-compatibility requirement, not optional).

**Schema.**
```sql
create table price_list_versions (
  id uuid primary key default gen_random_uuid(),
  price_list_id uuid not null references price_lists(id) on delete cascade,
  version_number int not null,
  status text not null check (status in ('draft','scheduled','active','expired','archived')),
  effective_date date,
  notes text,
  created_by uuid not null references auth.users(id), created_at timestamptz not null default now(),
  published_at timestamptz, published_by uuid references auth.users(id),
  unique (price_list_id, version_number)
);
create unique index price_list_versions_one_active on price_list_versions (price_list_id) where status = 'active';
```
- Add `price_list_prices.price_list_version_id` (additive column, migrate, then drop the old `price_list_id` column in a later sub-step within this phase once nothing reads it — never a single risky rename).
- Data migration: one `price_list_versions` row per existing `price_lists` row (`version_number=1, status='active', effective_date=today`), repoint existing `price_list_prices` rows at it.
- `companies.price_list_id` **stays pointed at the logical `price_lists.id`**, not a version — add a `current_price_list_prices(price_list_id)` view/function (security invoker, joins `price_list_versions where status='active'`) that `applyEffectivePricing`'s callers read instead of `price_list_prices` directly.
- Immutability: a trigger on `price_list_prices` rejecting writes against a row whose version is `status='active'`.
- New RPCs: `admin_create_draft_version(p_price_list_id, p_from_version_id default null)`, `admin_set_draft_price(p_version_id, ...)` (replaces `admin_set_price_list_price` for version-scoped writes — cut over atomically in this same commit, don't run two parallel write paths into the same table).
- New permission keys: `price_lists.create_draft`, `price_lists.publish`, `price_lists.schedule`.
- **Scheduled-activation decision** (Phase 8's concern, but the schema needs to support whichever is chosen): lean lazy on-read check (`effective_date <= current_date` in the "active version" resolution logic) over introducing `pg_cron` as a new dependency — no existing precedent either way, this is a from-scratch call.

**Frontend.** Minimal — only what keeps `AdminPriceListsPage.tsx` compiling/working against the new RPC names (its "edit in place" UX now implicitly targets that list's current draft, auto-creating one via `admin_create_draft_version` on first edit if none exists — a stopgap until Phase 7's real draft/compare/publish UX).

**Dependencies.** None from the accounts module, but this is a live-table production migration — highest care of any phase.

**Verification.** Standard triad; new pgTAP file for the immutability trigger + one-active-version-per-list invariant; live Playwright pass confirming `AdminPriceListsPage.tsx` still works end-to-end (edit price, CSV import/export) after cutover, not just typechecks.

**What actually shipped.**
- **Schema** (new "Pricing: Price List Versioning (Company Accounts & Pricing, Phase 6)" section in `supabase/schema.sql`, right after the pricing tables' FK-indexes section): `price_list_versions` table + `price_list_versions_one_active` partial unique index, exactly as planned. Data migration seeds one `version_number=1, status='active'` row per existing `price_lists` row, adds `price_list_prices.price_list_version_id` (nullable), backfills it, sets it `not null`, then drops the old `price_list_prices_unique` index, the old `idx_price_list_prices_price_list_id` FK index, and the `price_list_id` column itself, replacing them with a version-scoped unique index + FK index. The "Company members can read their assigned list's prices" RLS policy is dropped and recreated (had to happen *before* the column drop — Postgres won't drop a column a policy still references) to join through `price_list_version_id` and require `status = 'active'`, closing the real gap noted in the plan: before this phase there was only ever one version per list, so a company member reading "their list's prices" and "their list's *active* prices" were the same query; now that drafts exist, the old policy body would have let a company member read an in-progress draft's prices, which the rewrite prevents. Two new RLS policies on `price_list_versions` itself ("Staff can read...", "Active price list versions are readable by any authenticated user") support this and `current_price_list_prices()`'s invoker-security read.
- **Immutability trigger** (`guard_price_list_prices_immutable`) fires on `UPDATE OR DELETE` only, deliberately not `INSERT` — every insert path into `price_list_prices` is already RPC-gated (no direct-insert RLS policy exists at all) and the only RPCs that insert into an already-existing version either create that version in the same statement (`admin_create_price_list`/`admin_duplicate_price_list`, populating a brand-new active version, not mutating one) or explicitly require `status = 'draft'` first (`admin_set_draft_price`) — guarding `INSERT` too would only have blocked that legitimate initial-population path. Placed after the data-migration `UPDATE` and the `NOT NULL`, per the plan's own ordering note.
- **RPCs**: `current_price_list_prices(p_price_list_id)` (the planned view/function, built as a `security invoker` function rather than a view — same effect, and function felt closer to this file's existing RPC-heavy style than introducing this schema's first application-facing view). `admin_create_draft_version(p_price_list_id, p_from_version_id default null)` and `admin_set_draft_price(...)` exactly as planned. `admin_set_price_list_price`/`admin_delete_price_list_price` dropped outright (cut over atomically, no parallel write paths, per the plan). One deviation from the plan's literal RPC list: **`admin_delete_draft_price(p_id)`** was added — not explicitly named in the plan, but `admin_delete_price_list_price` needed *some* version-scoped replacement for `AdminPriceListsPage.tsx`'s existing "clear price" action to keep working, and `admin_set_draft_price` alone doesn't cover deletion. `admin_create_price_list`/`admin_duplicate_price_list` (both `plpgsql`, edited in place — checked against referenced objects only at *call* time, so referencing `price_list_versions` before its own `CREATE TABLE` further down the file is fine) now also create/copy an initial active version. `admin_list_price_lists` (`language sql`, checked at *CREATE* time) couldn't be edited in place at its original definition — it's `create or replace`d a second time, later in the file after `price_list_versions` exists, with `product_count` corrected to count only the active version's prices (a draft-in-progress no longer inflates the count shown in the library table).
- **Permission keys**: `price_lists.create_draft`/`.publish`/`.schedule` seeded now, `publish`/`schedule` unused until Phase 8 lands their RPCs — same "seed the key before its RPC" precedent this file's nav keys already follow. None granted to any staff role (matching every other `price_lists.*` key's super-admin-only posture) — `admin_set_draft_price`/`admin_delete_draft_price` deliberately reuse the existing `price_lists.set_price`/`.delete_price` keys rather than minting new ones, since they're the direct version-scoped replacements for the RPCs that used to check those same keys.
- **Frontend**: `priceListTypes.ts`'s `PriceListPriceRowSchema.price_list_id` renamed to `price_list_version_id` (mechanical — nothing reads `.price_list_id` off a row directly). `priceListsStore.ts`'s `useAdminPriceListPrices` rewritten to resolve an existing draft-or-active version on load (preferring an in-progress draft so re-visiting a list you're mid-editing doesn't lose your place), and to auto-fork a draft via `admin_create_draft_version` on the *first* write if none exists yet (subsequent edits within the same session reuse that same draft, verified live — see below). `deletePrice` needed the most care: a row's `id` belongs to whichever version was last resolved, so deleting while still viewing the active version forks a draft first (copying every price into fresh rows with new ids) and then re-finds the matching row in the new draft by product identity (category + the one non-null panel/track/fixing/sealant id), since the caller's stale active-version id no longer identifies anything post-fork. `useEffectivePriceListPrices` (the one hook `applyEffectivePricing`'s real callers — `OrderBuilderPage.tsx`/`QuickOrderPage.tsx` — actually use) swapped its two `price_list_prices` selects for two `current_price_list_prices()` RPC calls, so customer-facing order pricing only ever sees a list's active version, never a draft. `priceListCsv.ts` and `CompanyPriceListCard.tsx` needed zero changes, as the plan predicted (both schema-agnostic to this cutover). Added one small UI piece the plan left open as "consider": a "Draft in progress" badge on `AdminPriceListsPage.tsx`'s detail header (only rendered when `versionStatus === "draft"`), since editing a price no longer goes live immediately post-Phase-6 — a real, deliberate behavioral change from before (edits now target an unpublished draft until Phase 8's publish flow exists), and admins need *some* signal they're not looking at what customers currently see.
- New `supabase/tests/database/11_price_list_versions.test.sql` (20 pgTAP assertions): the immutability trigger rejects both direct `UPDATE` and `DELETE` against an active-version price row (run as the unrestricted connecting role, since `price_list_prices` has no write RLS policy at all — a raw client write would already be blocked by RLS before ever reaching the trigger; this proves the trigger itself works, independent of that first line of defense); `admin_create_price_list()`'s auto-created initial version; `admin_set_draft_price()`/`admin_delete_draft_price()` both rejecting a target that isn't a draft and succeeding against a real one; the one-active-version invariant surviving a draft's creation, plus a raw-insert defense-in-depth check that a second active version genuinely violates `price_list_versions_one_active` at the database level (not just via RPC logic); `current_price_list_prices()` and `admin_list_price_lists()`'s `product_count` both correctly excluding an unpublished draft's prices; permission gating for `dispatch` (no `price_lists.create_draft`/`.set_price` grant); and the RLS draft-visibility gap itself — a company owner can read their assigned list's *active* version metadata but not an in-progress draft's, and can still read the (unrelated, unaffected) default list's active prices. `05_execute_grants.test.sql` updated: removed the 2 stale assertions for the dropped RPCs, added 4 for the new ones, `plan()` bumped from 76 to 78.
- **A gap found (and fixed) in this sandbox's own local-verification recipe, not in the shipped code**: the manual grant-restoration step from prior phases' verification passes (blanket `grant select/insert/update/delete on all tables` + re-scraping every `revoke execute on function ...` line) missed schema.sql's one *column-level* `grant select (...) on order_deliveries to authenticated` line (immediately following its own table-level revoke) — this was already correct in schema.sql itself, just never previously exercised by a phase touching pricing. Restoring it fixed 2 previously-failing `07_delivery_allocation.test.sql` assertions that were unrelated to Phase 6 but had been silently failing in this sandbox; worth remembering for the next phase's verification pass. Also confirmed (unchanged from Phase 5's own note) 2 pre-existing, unrelated pgTAP failures remain: `02_company_isolation.test.sql`'s `plan(10)` vs. 11 actual assertions (a stale count in a file this phase never touched) and `06_dynamic_permissions.test.sql`'s "31 seeded grants" assertion (schema.sql seeds 32 today) — both predate this phase and are out of its scope.

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`, with fixture panel/track/fixing/sealant rows inserted directly since this repo carries no product-catalog seed data of its own): opened PL1 - Standard with no draft yet (no badge shown, 0 priced products, matching the corrected `product_count`); edited one panel's price — a draft was auto-created and the "Draft in progress" badge appeared immediately; edited a second panel's price — reused the same draft (confirmed via screenshot, no duplicate badge/second draft created); reloaded the page from scratch — the in-progress draft was correctly re-resolved (not lost, not re-forked); deleted the first panel's price — cleared back to "Not set" without disturbing the second panel's price; CSV export triggered a real download with no console errors; light and dark mode both screenshotted and correct, including the new badge. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors/warnings). `npm run test:rls`: the full manual schema.sql split-apply (including the `price_list_id` → `price_list_version_id` column drop and the SQL-language `admin_list_price_lists` redefinition-after-the-fact) applied with zero errors against a real Postgres, and `11_price_list_versions.test.sql`/the extended `05_execute_grants.test.sql` both pass cleanly; only the 2 pre-existing unrelated failures noted above remain.

---

## Phase 7 — Price Lists library + draft editor UI — DONE

**Scope.** New Price Lists page (Overview/Price Lists/Draft Versions/Scheduled/Archived tabs, library table, company-allocation breakdown) + price-list detail/draft editor (Product Prices/Details/Visibility/Company Impact/Publish tabs, version-diff table, draft-validation checklist, CSV import into a draft).

**Backend.** `admin_list_price_list_versions(p_price_list_id)`, `admin_diff_price_list_versions(p_from_version_id, p_to_version_id)` (per-product old/new price + Changed/Unchanged — build once, reuse in Phase 8's Compare screen). Validation (duplicate lines impossible given the unique index; negative/zero and large-change-% detection) can be pure client-side over the diff RPC's output.

**Frontend.** `src/pages/accounts/priceLists/PriceListsPage.tsx`, `PriceListVersionEditor.tsx` (reuses `priceListCsv.ts`'s `parsePriceListCsv`/`exportPriceListCsv` verbatim, only `priceListsStore.ts`'s param needs to shift from `price_list_id` to `price_list_version_id`), `priceListVersionsStore.ts`.

**Dependencies.** Phase 6.

**Verification.** Standard triad; live Playwright pass: create draft → edit prices → CSV import into it → confirm the active version is genuinely read-only in the UI (not just server-rejected).

**What actually shipped.**
- **Backend** (new "Pricing: Price Lists library + draft editor" section in `supabase/schema.sql`, right after Phase 6's `admin_list_price_lists()` redefinition): both RPCs built exactly as planned, both reusing the existing `price_lists.read` permission key rather than minting new ones. `admin_list_price_list_versions(p_price_list_id default null)` — null returns every version across every list (backs the library's cross-list Draft Versions/Scheduled/Archived tabs), a specific id scopes to one list's own version history (the editor's Details tab); `created_by_name`/`published_by_name` reuse `admin_list_companies()`'s own display-name-or-email lateral-join pattern. `admin_diff_price_list_versions(p_from_version_id, p_to_version_id)` is a full outer join on `(category, coalesce(panel_id, track_id, fixing_id, sealant_id))` between two versions' price rows — needed because `price_list_prices.id` isn't stable across versions (each version's rows are its own copy, per `admin_create_draft_version()`). It deliberately returns bare category/product-id columns, not resolved product labels — the frontend already holds the full catalog client-side (`useProductStore()`) and looks labels up against it via `itemTitle()`, so the RPC stays a thin numeric diff.
- **`priceListVersionsStore.ts`**: `useAdminPriceListVersions(priceListId)` and `useVersionDiff(fromVersionId, toVersionId)`, following the existing `invitationsStore.ts` convention of inline zod row schemas rather than a shared types file. All writes (create/rename/duplicate/delete a list, create a draft, set/delete a draft price) reuse Phase 6's `priceListsStore.ts` hooks directly — nothing duplicated.
- **`PriceListsPage.tsx`**: Overview/Price Lists/Draft Versions/Scheduled/Archived tabs. "Price Lists" is the library table (`useAdminPriceLists()`, the same Phase 6 hook `AdminPriceListsPage.tsx` uses); the other three status tabs share one `VersionsTable` over `useAdminPriceListVersions(null)`, filtered client-side by status. Scheduled/Archived render genuinely empty (no version is ever created with either status until Phase 8's publish flow exists) — that's the honest state to show, not something to faked data for. Per-company breakdown lives one level down on a list's own Company Impact tab, not duplicated here.
- **`PriceListVersionEditor.tsx`**: Product Prices tab reuses the exact `AdminPriceListsPage.tsx`/Phase 6 category-chips + search + price-row + CSV import/export pattern (a new, page-local copy, not an extracted shared component — the two pages deliberately coexist until Phase 14 retires the old one, so premature extraction wasn't worth the risk). Details tab: rename/duplicate/delete + this list's own version history table. Visibility tab: informational copy on who can see published-vs-draft prices (staff always; default list's active prices readable by any authenticated user; a non-default list's active prices only by its explicitly assigned companies) — no new backend needed, this is really just documenting the RLS policies Phase 6 already wrote. Company Impact tab: filters the existing `useAdminCompanies()` rows by `price_list_id`, no new RPC. Publish tab: the version-diff table (active vs. draft) plus a client-side validation checklist (prices at zero/below, prices changed >20%, count of removals) — the actual `Publish` button is present but disabled ("Coming in Phase 8"), since `admin_publish_price_list_version()` doesn't exist yet; this tab is a preview, not a working publish flow, exactly matching the plan's own phase split.
- **Real bug caught during live verification, fixed before shipping**: the header's "Draft in progress" badge and the Publish tab's diff both read `useAdminPriceListVersions()` — a *separate* hook instance from the one `useAdminPriceListPrices()` (Phase 6) uses internally to auto-fork a draft on first edit. Editing a price correctly created the draft server-side, but the badge and Publish tab kept showing stale "no draft" state until a full page reload, since nothing told the versions hook to refetch. Fixed by lifting `useAdminPriceListVersions()` to the top-level `PriceListVersionEditor` component (shared by every tab via props, not called separately inside `DetailsTab`/`ProductPricesTab` anymore) and adding an `onChanged` callback `ProductPricesTab` fires after every successful price write/CSV import, which the parent wires to the lifted hook's `reload`. Confirmed live: editing a price now shows the badge and a correct Publish-tab diff immediately, without a reload.
- `useHashRoute.ts`: `priceListId?: string` added to the `accounts` route (mirrors `companyId`) for `#/accounts/priceLists/<id>` drilling into one list's editor. List *creation* stays an inline form on `PriceListsPage.tsx` itself (matching `AdminPriceListsPage.tsx`'s existing precedent), not a separate route the way `companies`' `newCompany` is. `AccountsRoot.tsx`'s `priceLists` sub-page is real now (was a `PlaceholderPage`) — `AccountsRoot`/`App.tsx` also needed a new `layoutMode` prop threaded through (matching `AdminRoot.tsx`'s existing pattern), since the editor's Product Prices tab needs it for `CardGrid`.
- No new pgTAP file — both new RPCs are read-only (no RLS/write-path surface to test beyond what `11_price_list_versions.test.sql` already covers for the underlying tables), gated by the already-tested `price_lists.read` key. `05_execute_grants.test.sql` extended for the 2 new RPCs (`plan()` bumped 78 → 80).
- **This phase's branch history**: PR #126 (Phases 1-6) merged before this phase started, with its own pgTAP CI check still failing — root-caused (not by this phase's work) to the branch predating a separate, concurrently-merged PR #127 that fixed this repo's local/CI Supabase bootstrap (baseline grants, a `supabase/migrations/00000000000000_schema.sql` symlink so `schema.sql` is actually discovered, `price_lists.created_by` made nullable for a fresh bootstrap, `seed.sql`'s `\set`-inside-`do $$ $$` fix, and the two stale pgTAP plan counts noted in Phase 6's own writeup). This phase's branch was restarted from latest `main` (already carrying both merged PRs) before any Phase 7 code was written, and one more nullable-column fix in the same vein landed on `main` between the two merges (`price_list_versions.created_by`, same root cause as `price_lists.created_by`) — already inherited by this branch, not something this phase needed to redo.

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`, with fixture panel/track/fixing/sealant rows inserted directly since this repo carries no product-catalog seed data of its own): library page's KPI tiles and all 5 tabs render correctly (Overview's empty-drafts state, Price Lists' table, Draft/Scheduled/Archived all correctly empty pre-edit); opened PL1 - Standard, edited a price (auto-forked a draft, badge appeared immediately — confirming the bug fix above); Details tab's version history table showed both v1 (active, 0 priced products) and v2 (draft, 1 priced product) with correct `created_by` attribution; Visibility tab's copy rendered; Company Impact tab correctly listed both seeded companies; Publish tab showed the correct diff (`P51 Panel`, Added, $88.50) and checklist (0/0/1), with `Publish` genuinely disabled; the cross-list Draft Versions tab back on the library page picked up the new draft without a manual refresh. Light and dark mode both screenshotted and correct, zero console errors throughout. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors). `npm run test:rls`: all 11 pgTAP files pass cleanly (`Result: PASS`, 183 assertions) — the two previously-failing pre-existing files (`02_company_isolation.test.sql`, `06_dynamic_permissions.test.sql`) are green now too, since PR #127's fixes for both are already on `main`.

---

## Phase 8 — Compare & Publish — DONE

**Scope.** Version-diff table with company-impact counts, Normal/Review flag per line (large % change), publication-approval note, Publish action, and the scheduled-activation decision from Phase 6 actually implemented (lazy on-read check, recommended).

**Backend.** `admin_publish_price_list_version(p_version_id, p_approval_note)` — validates no concurrent publish in flight, flips outgoing active version to `expired`, target to `active` (immediately) or leaves at `scheduled` (future `effective_date`, resolved via the lazy-check function/view at read time — no cron job). Logs `price_list_version_published` audit event with company-impact counts in `detail`.

**Frontend.** `src/pages/accounts/priceLists/ComparePublishPage.tsx`.

**Dependencies.** Phase 7.

**Verification.** Standard triad; pgTAP test that the one-active-version invariant survives a publish (never two actives, never zero visible mid-transaction); live Playwright pass through create→edit→compare→publish→confirm old version `expired`, new one `active`, and Phase 6's `AdminPriceListsPage.tsx` stopgap targets a fresh draft again.

**What actually shipped.**
- **Backend** (new "Pricing: Compare & Publish" section in `supabase/schema.sql`, right after Phase 7's `admin_diff_price_list_versions()`): `admin_publish_price_list_version(p_version_id, p_effective_date default null, p_approval_note default null)` — reuses the `price_lists.publish` permission key seeded (unused) back in Phase 6. Rejects a non-draft target (`Only a draft version can be published`) and rejects outright if the list already has a *pending* `scheduled` version (`This price list already has a scheduled publish pending`) — "no concurrent publish in flight" resolved as "at most one pending future activation per list at a time." `p_effective_date` null/today-or-past publishes immediately (outgoing active version flipped to `expired` **first**, target flipped to `active` **second** — the `price_list_versions_one_active` partial unique index isn't deferrable, so doing it in the other order would momentarily violate it, 1→2 instead of 1→0→1); a future date instead marks the target `scheduled` and **deliberately leaves the current active version untouched** — no gap where a list has zero effectively-active prices while waiting for a scheduled date to arrive.
- **The scheduled-activation "lazy on-read check," resolved as a from-scratch design call exactly as Phase 6 flagged it**: a new `current_price_list_version_id(p_price_list_id)` (security invoker) resolves "the one version currently in effect" — literally `active`, or if none, the most recent `scheduled` version whose `effective_date` has already passed, with a deterministic tie-break (the due-scheduled version always wins over a stale literal-active one) so it never blends two versions' prices together. `current_price_list_prices()` redefined a third time (same `language sql`-checked-at-CREATE-time reason every prior redefinition in this file follows) to join through this resolver instead of a plain `status = 'active'` filter. Both RLS policies that reference version status (`price_list_versions`' "Active..." policy, `price_list_prices`' "Company members..." policy) got the same `status = 'active' or (status = 'scheduled' and effective_date <= current_date)` OR-clause added (drop+recreate, same convention every prior phase's schema changes follow) — required because `current_price_list_prices()` is invoker and can't see rows RLS doesn't already let the caller see. **Deliberately scoped no further than this**: `admin_list_price_lists()`'s `product_count` and `admin_list_price_list_versions()`'s `status` column both keep showing the *literal* stored value (a scheduled-and-due version still displays "Scheduled" in the admin UI until something republishes it) — only customer-facing pricing needed to be lazily correct, extending the same logic into every admin display/count would have meant a lot more surface area for comparatively little value, an explicit scope call documented in the schema comment.
- **Audit logging**: `log_audit()` is inherently per-company (silently no-ops on a null `company_id`), and a price list isn't itself scoped to one company — so `admin_publish_price_list_version()` logs one `price_list_version_published` row per company currently assigned to the list (their own Activity Log is exactly where "your price list changed" belongs) rather than reworking `log_audit`'s shape or skipping audit logging entirely. Added the label to `EVENT_TYPE_LABELS` now rather than deferring to Phase 13 — same "seed early, opportunistically" precedent `company_status_changed` already set back in Phase 2 despite Phase 13's own text nominally listing it as a Phase-13 concern.
- **`ComparePublishPage.tsx`**: grew out of Phase 7's inline `PublishTab` (which only had the diff table + a checklist summary + a permanently-disabled button) into its own file, adding: a company-impact count, a per-row Normal/Review flag column (not just a checklist tally), an optional effective-date picker (blank = publish now, a future date schedules), an approval-note textarea, a confirm dialog whose copy changes based on immediate-vs-scheduled, and the real working Publish button. `PriceListVersionEditor.tsx`'s Publish tab now renders this instead of the old inline component.
- `priceListVersionsStore.ts` gained `adminPublishPriceListVersion(versionId, effectiveDate, approvalNote)`.
- New `supabase/tests/database/12_publish_price_list_version.test.sql` (19 pgTAP assertions): the non-draft-publish rejection; immediate publish flipping outgoing→`expired`/target→`active` with the one-active invariant holding; scheduling a future version while the current active stays untouched; the concurrent-scheduled-publish rejection; and the lazy resolution itself — simulating a scheduled version's date having passed (a direct fixture update, since there's no cron job to wait on) and confirming `current_price_list_version_id()`/`current_price_list_prices()` correctly resolve to it over the stale literal-active version, **and** that a non-staff company owner can read it directly via the RLS OR-clause (not just via the invoker function). One test-construction bug caught along the way: resolving a non-default price list's id via `price_lists.name` while impersonating a non-staff company owner silently returns nothing (that table's own RLS only exposes a non-default list to staff) — fixed by resolving via `companies.price_list_id` instead, the same pattern `11_price_list_versions.test.sql` already used for its own company-owner checks. `05_execute_grants.test.sql` extended for the 2 new RPCs (`plan()` bumped 80 → 82).

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`, with fixture panel/track/fixing/sealant rows inserted directly): edited a price on PL1 - Standard (auto-forked a draft), opened the Publish tab — checklist/company-impact count ("2 companies assigned to this list today")/diff table with a correct "Added"/"Normal" row all rendered; clicked Publish now → confirm dialog showed the correct "affecting 2 companies" copy → confirmed → draft badge disappeared immediately (no reload needed, `onPublished` correctly triggers the lifted `reloadVersions`); Details tab's version history confirmed v1 `expired` / v2 `active`, both with correct `PUBLISHED` dates; edited another price afterward and confirmed a **fresh** draft (v3) auto-forked again, badge reappeared — the exact "Phase 6's stopgap targets a fresh draft again" behavior the plan's own verification note calls for. Zero console errors. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (187/187 tests, 0 new depcruise errors). `npm run test:rls`: all 12 pgTAP files pass cleanly (`Result: PASS`, 204 assertions) against a real local Postgres via a full `supabase db reset` (no manual split-apply needed this time — `main`'s inherited CI-bootstrap fixes handle it end to end).

---

## Phase 9 — Method 2: item-level company price overrides — DONE

**Scope.** `company_price_overrides` table + Company Pricing tab (override table, pricing-priority explainer, live Customer Price Preview, expiry warning) + the 3-tier priority actually wired into pricing resolution.

**Backend.**
```sql
create table company_price_overrides (
  id uuid primary key default gen_random_uuid(),
  company_id uuid not null references companies(id) on delete cascade,
  category text not null check (category in ('panel','track','fixing','sealant')),
  panel_id uuid references panels(id), track_id uuid references tracks(id),
  fixing_id uuid references fixings(id), sealant_id uuid references sealants(id),
  override_price numeric not null,
  effective_date date not null default current_date,
  expiry_date date,
  internal_reason text,
  created_by uuid not null references auth.users(id), created_at timestamptz not null default now(),
  approved_by uuid references auth.users(id), approved_at timestamptz,
  check ( /* same one-of-four-FKs pattern as price_list_prices */ )
);
create unique index company_price_overrides_unique on company_price_overrides
  (company_id, category, coalesce(panel_id, track_id, fixing_id, sealant_id))
  where expiry_date is null or expiry_date >= current_date;
```
Active/Scheduled/Expired is derived from `effective_date`/`expiry_date` at read time, never stored (matches the mockup's own description — only genuinely stateful lifecycles like `price_list_versions.status` get an explicit column). New RPCs: `company_list_price_overrides(p_company_id)`, `admin_set_company_price_override`, `admin_delete_company_price_override`. New permission keys `company_price_overrides.read`/`.write`.

**`applyEffectivePricing.ts` gets its 3rd, highest-priority tier**: `override ?? assignedList ?? fallback ?? existing`. Audit and update every current caller (pricing preview, order pricing, Excel export), not just the obvious one.

**Frontend.** `src/pages/accounts/companies/CompanyPricingTab.tsx`, `companyPriceOverridesStore.ts`, `PricingPriorityExplainer.tsx`, `CustomerPricePreview.tsx`.

**Dependencies.** Phase 2 (Overview shell); land after Phase 6 so "assigned price list" correctly means "assigned list's active version."

**Verification.** Standard triad; pgTAP for the one-active-override-per-product index + cross-company isolation; extend the existing `applyEffectivePricing.test.ts` with an override-present case; live Playwright pass setting an override and confirming both the Customer Price Preview and a real estimate/order reflect it.

**What actually shipped.**
- **The plan's own literal partial-unique-index sketch doesn't work in Postgres, discovered empirically before writing any real schema**: `where expiry_date is null or expiry_date >= current_date` in a partial index predicate fails with `functions in index predicate must be marked IMMUTABLE` (confirmed via a throwaway `docker run postgres:17` container, then cleaned up) — `current_date` isn't immutable, unlike a plain column comparison. Substituted a `before insert or update` trigger (`guard_company_price_overrides_no_overlap()`) enforcing the identical invariant (no second current-or-upcoming override for the same company+category+product) procedurally instead, documented directly in the schema comment so a future reader doesn't rediscover the same dead end. `company_price_overrides` itself matches the plan's sketch closely otherwise: the same one-of-four-FKs `check` pattern `price_list_prices` already uses, `internal_reason`/`created_by`/`approved_by`/`approved_at` as audit-only metadata (no pending-approval workflow, per the plan's own resolved-decision note), Active/Scheduled/Expired derived from `effective_date`/`expiry_date` at read time rather than stored.
- **New RPCs** (new "Company Price Overrides — Method 2 pricing" section in `supabase/schema.sql`, right after Phase 8's `admin_publish_price_list_version()`): `current_company_price_overrides(p_company_id)` (security invoker, narrow customer-facing column set, only currently-active rows — mirrors `current_price_list_prices()`'s own "resolve what's in effect right now" shape); `company_list_price_overrides(p_company_id)` (security definer, every row regardless of status, full columns including `internal_reason`/resolved `created_by_name`/`approved_by_name`, gated by `public.has_permission('company_price_overrides.read')` folded into its own `where` clause rather than an early `raise` — a caller lacking the permission gets zero rows, not an error, matching the plan's "staff module" framing); `admin_set_company_price_override(...)` (upsert-by-lookup rather than `ON CONFLICT`, since the immutable-index-predicate discovery above means there's no unique index to hang one off — looks up any current-or-upcoming row for the same product and updates it in place, else inserts); `admin_delete_company_price_override(p_id)`. Both writes gated by a new `company_price_overrides.write` permission key; both log a per-company `item_override_added`/`item_override_changed`/`item_override_removed` audit event (added to `EVENT_TYPE_LABELS` immediately, same "seed early, opportunistically" precedent `company_status_changed`/`price_list_version_published` already set rather than waiting for Phase 13).
- **Column-level visibility split**: `internal_reason`/`created_by`/`approved_by`/`approved_at` are hidden from every direct `authenticated` table read (including a company's own RLS-visible rows) via the exact `revoke select on ... from authenticated; grant select (<narrow list>) on ... to authenticated;` technique `order_deliveries.internal_note` already established — those columns remain fully visible inside `company_list_price_overrides()` (security definer, reads as the table owner, bypassing the column grant the same way it bypasses RLS). This column-level restriction applies per *Postgres role*, not per JWT claim: even a super_admin session (still physically connected as the `authenticated` role, just with different `has_permission()`-visible claims layered on) cannot select `internal_reason` directly — only through the security-definer RPC. Discovered while writing the pgTAP coverage below and worth flagging for any future phase that reaches for the same column-grant pattern.
- **RLS**: `"Staff can read company price overrides"` (`has_permission('company_price_overrides.read')`, every row) and `"Company members can read their company's active overrides"` (active membership + currently-in-effect dates only) — mirrors the staff-vs-customer read split every prior pricing table in this module already uses.
- **`applyEffectivePricing.ts`'s 3rd, highest-priority tier**: signature grew from `(catalog, assignedListRows, defaultListRows)` to `(catalog, overrideRows, assignedListRows, defaultListRows)`, resolution now `overrides.get(...) ?? assigned.get(...) ?? fallback.get(...) ?? existing`. New `CompanyPriceOverrideRowSchema`/`overrideRowProductId()` added to `priceListTypes.ts` (not a new file) since both `applyEffectivePricing.ts` and `priceListsStore.ts` need the shape. **Audited every current caller, per the plan's explicit instruction**: `useEffectivePriceListPrices(companyId)` (the one hook both real call sites use) now also fetches `current_company_price_overrides()` alongside its existing assigned-list/PL1 fetches; `OrderBuilderPage.tsx` and `QuickOrderPage.tsx` (still the only two real call sites — the Excel-export paths read already-frozen order line items, not a live catalog, so they're out of scope until Phase 10's price-freeze work) both updated to thread the new `overrides` value through. `applyEffectivePricing.test.ts` gained an override-precedence test case plus an `overrideRow()` fixture helper alongside the existing `priceRow()`.
- **Frontend**: `CompanyPricingTab.tsx` (override table with Product/Category/Override price/Effective/Expiry/Status/Reason/Added-by/actions columns, an amber expiry-warning banner for overrides due within 14 days, add/edit form disabling the category/product selects once editing — changing a product mid-edit would upsert against a *different* override and leave the one being edited untouched, so it's blocked at the UI rather than surprising the admin), `companyPriceOverridesStore.ts` (`useCompanyPriceOverrides`/`adminSetCompanyPriceOverride`/`adminDeleteCompanyPriceOverride`/`overrideLifecycle()`/`overrideProductId()`), `PricingPriorityExplainer.tsx` (static 3-tier reference card, no data fetch), `CustomerPricePreview.tsx` (reuses `useEffectivePriceListPrices()` + `applyEffectivePricing()`'s own resolution logic rather than a parallel re-implementation, so the preview can never drift from real order pricing — shows every product's resolved price plus which tier produced it: Item override/Assigned list/PL1/Catalog default). Wired into `CompanyOverviewPage.tsx`'s Pricing tab (removed from `OTHER_TABS`' `PlaceholderPage` list, same treatment Addresses/Users already got).
- **A real stale-hook bug found and fixed during Playwright verification**: `CustomerPricePreview` runs its own `useEffectivePriceListPrices(companyId)` instance, entirely separate from `CompanyPricingTab`'s own override list/reload — deleting an override updated the override table correctly but the live preview kept showing the deleted override's price until an unrelated re-render happened to remount it. Fixed by bumping a `previewRefreshToken` state value (incremented on every successful add/edit/delete) used as `CustomerPricePreview`'s React `key`, forcing a fresh mount/fetch after every write — the same "sibling hook instance doesn't know a write happened" problem Phase 7's `PriceListVersionEditor.tsx` hit and fixed via an `onChanged` callback lift; solved via remount here instead since `CustomerPricePreview` has no `reload()` to call into.
- New `supabase/tests/database/13_company_price_overrides.test.sql` (29 pgTAP assertions): the no-overlap guard trigger exercised via a raw insert as the unrestricted role (the table has no `authenticated` insert/update/delete policy at all — every write goes through the SECURITY DEFINER RPCs — so a raw insert as `authenticated` would be rejected by RLS before ever reaching the trigger); an expired existing override *not* blocking a new one for the same product, proving the guard is date-aware, not a blanket one-per-product rule; the same product for a different company inserting cleanly, proving the guard is company-scoped; `admin_set_company_price_override()`'s upsert-by-lookup (updating the existing current row in place vs. a genuine insert for a different product); invalid-category and expiry-before-effective-date rejections; the non-staff permission gate on both write RPCs; the staff-vs-customer read split (`company_list_price_overrides()` returning every row including `internal_reason` for staff vs. zero rows for non-staff, `current_company_price_overrides()`/a direct table read narrowing to active-only rows for a company member); cross-company isolation (Company B's owner reading zero rows against Company A, via both the RPC and a direct RLS-gated table read); the column-grant restriction (a raw select naming `internal_reason` rejected with `42501` even for a super_admin session, confirming column grants apply per Postgres role rather than per JWT claim — see the finding above); `admin_delete_company_price_override()` succeeding and rejecting a nonexistent id. `05_execute_grants.test.sql` extended for the 4 new RPCs (`plan()` bumped 82 → 86).

**Verified live** (Playwright against a local Supabase instance, `admin@e2e.test`, with a fixture panel row inserted directly — panels/tracks/fixings/sealants ship with no seed data in this repo, same as every prior phase's own verification note): opened Company A's new Pricing tab — the priority explainer, empty override table, and Customer Price Preview (correctly showing the fixture panel's $100 catalog-default price, tagged "Catalog default") all rendered correctly in both light and dark mode; added a $12,345 override — it appeared in the override table immediately and the Customer Price Preview updated to show $12,345.00 tagged "Item override" (confirming the live wiring through `useEffectivePriceListPrices()`/`applyEffectivePricing()`, the exact same path a real order uses); deleted the override — the table correctly emptied, and after fixing the stale-hook bug above, the preview correctly reverted to $100.00/"Catalog default". Zero console errors. `npm run typecheck && npm test && npm run build && npm run depcruise` all clean (188/188 unit tests, 0 new depcruise errors). `npm run test:rls`: all 13 pgTAP files pass cleanly (`Result: PASS`, 237 assertions) against a real local Postgres via a full `supabase db reset`.

---

## Phase 10 — Order price freeze

**Scope.** Close the confirmed gap: order creation is a bare RLS insert with no server-side price re-verification (the schema's own comment already flags this as a known, pre-existing tampering/staleness gap). Address freezing needs **no work** — `order_deliveries` already stores addresses as plain text, not a live FK.

**Backend.**
- Extend `orders.line_items` jsonb element shape additively: `priceSource: "override"|"price_list"|"default"`, `priceListVersionId`, `overrideId` (all optional — old rows without them still parse fine, matching this app's existing "patch missing fields on read" precedent from the estimator merge).
- New `orders.price_list_version_id` column (nullable), set at creation time.
- **Convert order creation from a bare RLS insert to an RPC** (`create_order(p_project_id, p_line_items, ...)`) that re-resolves each line item's price server-side against `company_price_overrides`/the assigned list's active version/default, rejecting (or flagging `matched: false`, mirroring the existing pattern) a client-submitted price that doesn't match — this is the actual fix, not a smaller trigger-based patch, since there's currently no RPC boundary at all for initial creation to hook into. This is a bigger, riskier change than the rest of this phase; consider splitting it into its own sub-phase if it grows.
- `revise_order` (existing RPC) gets the same re-resolution check added.

**Frontend.** Small, low-risk: show price source on order/quote-review UI (`OrderReviewDrawer.tsx`/`projectOrderSheet.tsx`) — display-only addition, keep this phase backend-heavy.

**Dependencies.** Phase 6 (price_list_versions), Phase 9 (company_price_overrides).

**Verification.** Standard triad; dedicated pgTAP file — this is the phase closing a documented security/correctness gap, so test that a fabricated client-sent `unitPriceExGst` gets rejected/corrected server-side; live Playwright pass placing an order, then changing the assigned list's active version or an override, confirming the already-placed order's displayed pricing is unchanged.

---

## Phase 11 — companies.status enforcement (On Hold / Suspended / Pending)

**Scope.** Make `companies.status` (expanded in Phase 2) actually gate something: On Hold/Suspended block new quote/order creation while preserving read access to existing projects/order history; Pending drives the onboarding-progress stepper; a real hold audit trail (reason/review-date, mockup screen 14).

**Backend.**
- Extend `can_submit_orders(...)` with an early-exit clause: if the company's status is `on_hold`/`suspended`, return `false` regardless of the existing owner/admin checks (layer on top, don't rewrite).
- `companies` gains `hold_reason text`, `hold_applied_by uuid`, `hold_placed_at timestamptz`, `hold_review_date date` (nullable, populated only while `status='on_hold'`), set/cleared inside Phase 2's `admin_set_company_status` RPC.
- Onboarding completion as a computed function (`company_onboarding_progress(company_id)` returning per-step booleans: legal_name+abn present, has an owner membership, has ≥1 default address, has a price list or ≥1 override) rather than a stored/maintained percentage.

**Frontend.** Pending Setup stepper + activation checklist, On Hold banner + restrictions list + hold-details card on `CompanyOverviewPage.tsx`; the external-user-facing block message on the customer-facing side (`src/pages/company/`) — real customer-visible copy, review against spec carefully.

**Dependencies.** Phase 2, Phase 3 (addresses), Phase 9 (pricing) — the onboarding checklist needs both.

**Verification.** Standard triad; extend `03_project_and_order_access.test.sql`'s pattern with an On-Hold-company order-blocked case; live Playwright pass as both a staff user (placing a hold) and an external customer of that company (confirm read access intact, new-order/quote creation blocked with correct message).

---

## Phase 12 — Access Permissions (curated view)

**Scope.** The curated per-role capability grid (screen 11) on top of existing `role_permissions` data.

**Backend.** None new — reads `admin_list_permission_matrix()` (existing) plus every permission key seeded by prior phases.

**Frontend.** `src/pages/accounts/permissions/AccessPermissionsPage.tsx` + `capabilityLabelMap.ts` (translates `(permission_key, role) → curated label`, defined once every prior phase's keys are known — why this phase is sequenced near the end), static "Pricing Protection" panel, static "Account Restrictions" panel (documents Phase 11's behavior).

**Dependencies.** Every phase adding a permission key (2, 3, 5, 6/7/8, 9, 11).

**Verification.** Standard triad; a real security review pass (not just functional click-through) — grep every RPC return shape and RLS policy reachable by a non-staff `company_memberships` role, confirm cost price/gross margin/supplier pricing/internal notes are genuinely never sent to an external-customer client, not just hidden in the UI.

---

## Phase 13 — Audit History (standalone) + pricing audit events + Transaction Price Trace

**Scope.** Cross-company Audit History page (searchable events, Transaction Price Trace sidecard, required-audit-data checklist) + the missing pricing-specific `EVENT_TYPE_LABELS` entries and their `log_audit()` call sites.

**Backend.** New event types at the right call sites: `price_list_assigned` (`admin_set_company_price_list`), `price_list_version_published` (Phase 8), `item_override_added/changed/removed` (Phase 9), `pricing_used_in_order` (Phase 10 — backs the Transaction Price Trace), `company_status_changed` (Phase 2/11), `company_address_added/changed/removed` (Phase 3). New cross-company `admin_list_audit_log(p_filters...)` RPC (today's is per-company only), new `audit.list_all` permission key. Transaction Price Trace reads directly off Phase 10's frozen `orders.line_items`/`price_list_version_id`/override fields — no new storage.

**Frontend.** `src/pages/accounts/audit/AuditHistoryPage.tsx`, `TransactionPriceTrace.tsx`.

**Dependencies.** Phases 9, 10, plus every phase contributing an event type.

**Verification.** Standard triad; live Playwright pass performing one action per earlier phase (add override, publish version, place order, put company on hold) confirming each produces the right audit row with actor+timestamp+old/new values.

---

## Phase 14 — Retirement of old pages + full mockup-parity audit

**Scope.** Cleanup, sequenced last on purpose (same reasoning as the estimator merge plan's own final phase — never delete a working fallback before its replacement has verified parity): retire `AdminCompaniesPage.tsx`/`AdminCompanyWizard.tsx`/`AdminPriceListsPage.tsx`'s old routes (delete or redirect, matching the existing `#/quote` redirect precedent in `useHashRoute.ts`), remove dead code (`admin_set_price_list_price` if fully superseded), and do a section-by-section visual audit against all 14 screenshots (web/iPad/phone, light/dark) the same way the estimator plan's mockup-parity audits worked.

**Dependencies.** All prior phases.

**Verification.** Full `typecheck/test/build/depcruise` + `npm run test:rls` + `npm run test:e2e`; full visual diff pass against every screenshot; confirm no dangling references to retired RPCs/components via `depcruise` + a grep sweep.

---

## Critical files

- `supabase/schema.sql` — companies/company_memberships/invitations/price_lists/price_list_prices/permissions/role_permissions/orders/order_deliveries sections
- `src/appShell/useHashRoute.ts`
- `src/pages/admin/AdminRoot.tsx`, `adminSections.tsx`, `adminSectionAccess.ts`
- `src/export/applyEffectivePricing.ts` (+ its test file)
- `src/pages/admin/priceLists/priceListTypes.ts`, `priceListsStore.ts`, `priceListCsv.ts`
- `src/pages/company/companyTypes.ts`, `staffTypes.ts`, `CompanyMemberList.tsx`
- `supabase/functions/company-invite-member/index.ts`
- `src/pages/admin/companies/AdminCompanyWizard.tsx`
- `src/pages/accounts/AccountsRoot.tsx`, `accountsTheme.css` (Phase 1 -- the new workspace's own shell)
- `supabase/tests/database/*.test.sql` (numbered convention, `npm run test:rls`)
- `e2e/*.spec.ts`, `e2e/fixtures/auth.ts` (`npm run test:e2e`)

## Verification (every phase)

`npm run typecheck && npm test && npm run build` (mandatory, CLAUDE.md standing rule) plus a live Playwright pass against a local Supabase instance (per CLAUDE.md's sandboxed-verification recipe) before considering any phase done. Add a numbered pgTAP file (`supabase/tests/database/NN_....test.sql`, run via `npm run test:rls`) for any phase touching RLS/RPCs. Extend `e2e/*.spec.ts` for any phase touching cross-company isolation or invitation delivery (`npm run test:e2e`, not CI-blocking today but still worth running locally). Phase 6 and Phase 10 are the two most likely to need splitting further once inside them — budget for that rather than forcing either into one commit.
